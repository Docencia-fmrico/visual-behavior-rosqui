<?xml version="1.0"?>
<root main_tree_to_execute="BehaviorTree">
    <!-- ////////// -->
    <BehaviorTree ID="BehaviorTree">
        <ReactiveSequence>
            <Fallback>
              <Condition ID="PercievePerson" person_x="{person_x}"/>
              <Action ID="Turn"/>
            </Fallback>
            <Action ID="FollowPerson" person_x="{person_x}"/>
        </ReactiveSequence>
    </BehaviorTree>
    <!-- ////////// -->
    <TreeNodesModel>
        <Condition ID="FollowPerson" person_x="{person_x}"/>
        <Action ID="PercievePerson" person_x="{person_x}"/>
        <Action ID="Turn"/>
    </TreeNodesModel>
    <!-- ////////// -->
</root>
